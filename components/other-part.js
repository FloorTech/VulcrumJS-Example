import { p } from "@floortech/vulcrumjs/dist/components/index.js"

export default function OtherPart() {
    return p("This part of the page is loaded from a separate file, but imported at build time!")
}
